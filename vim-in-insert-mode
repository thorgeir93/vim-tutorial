

      VIM - WORD COMPLETION
===============================

CTRL-N      - finds the next matching completion.
CTRL-P      - finds the previous matching completion.

CTRL-X-N    - finds the next matching completion in current file.
CTRL-X-P    - finds the next matching completion in current file.


CTRL-X-F    - finds the next matching filename.
CTRL-X-L    - finds the next matching line.


:help CTRL
